name: example
description: Flutter Flow Chart example
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_flow_chart:
    path: ../

  cupertino_icons: ^1.0.6

  # https://pub.dev/packages/star_menu
  star_menu: ^4.0.0

  # https://pub.dev/packages/path_provider
  path_provider: ^2.1.3

  # https://pub.dev/packages/flutter_colorpicker
  flutter_colorpicker: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:

  uses-material-design: true
